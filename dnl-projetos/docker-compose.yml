version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: dnl_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: ${KC_DB_PASSWORD:-keycloak_secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - dnl_net

  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    container_name: dnl_keycloak
    restart: unless-stopped
    command: start
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KC_DB_PASSWORD:-keycloak_secret}
      KC_HOSTNAME: ${KC_HOSTNAME:-localhost}
      KC_HTTP_ENABLED: 'true'
      KC_PROXY: edge
      KEYCLOAK_ADMIN: ${KC_ADMIN_USER:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KC_ADMIN_PASSWORD:-admin}
    ports:
      - '8080:8080'
    depends_on:
      - postgres
    networks:
      - dnl_net

  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: server
    container_name: dnl_api
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 3001
      DB_PATH: /data/dnl-projetos.db
    volumes:
      - sqlite_data:/data
    ports:
      - '3001:3001'
    depends_on:
      - keycloak
    networks:
      - dnl_net

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend
    container_name: dnl_frontend
    restart: unless-stopped
    ports:
      - '80:80'
    depends_on:
      - api
    networks:
      - dnl_net

volumes:
  postgres_data:
  sqlite_data:

networks:
  dnl_net:
    driver: bridge
